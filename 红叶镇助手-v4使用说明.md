# 红叶镇自动助手 v4.0.0

## 安装与面板

将 `红叶镇物语自动助手.user.js` 或同内容的 `red-leaf-town-helper.user.js` 更新到 Tampermonkey，然后刷新游戏页面。只需安装其中一份。

面板分为 **概览、生产、加工、航海、饲料、设置** 六页。顶部按钮控制整个助手，功能开关与参数自动保存在当前浏览器中。概览提供体力、金币、库存估值、生产进度、航海状态和饲料水位。

“设置”页可控制图形、日志、紧凑布局和刷新后自动启动。已开启的旧版功能沿用原有设置；航海默认关闭，新安装时饲料补充默认关闭。

## 加工

- **加工总开关**：关闭后停止自动提交、自动领取和伙伴派驻。游戏服务器中已经排入的队列继续执行。
- **自动提交队列 / 自动领取成品**：分别控制。可以只领取手动提交的队列。
- **批量加工**：默认每次最多 10 份，可设置 1～99。关闭后每次仅提交一份。实际数量还受步骤剩余份数、可用原料、体力保底和道具保留量约束。
- **流程**：每站最多 4 步，每步指定配方、份数和道具，点击“开始”执行。按实际领取份数推进；默认执行一轮即停，可开启循环。
- **锁定单一配方**：旁边的数量为本站总份数；0 表示持续生产，每次提交仍受批量上限限制。
- **本站允许提交**：可单独暂停某个站点的后续提交，不影响现有队列和自动领取。
- **道具**：总开关、加工开关、每步选择、保留量分别生效。开工道具不足且允许部分使用时，服务器只给队列前段使用。设置了非零道具保留量时，会缩小批量以保护保留部分。
- **取消本站队列**：由用户点击并确认后执行。当前加工的一份投入不退，未开始部分退回，已完成成品保留；本站后续提交自动暂停。

队列执行期间锁住流程编辑、配方切换和进度重置，避免新旧队列混算。旧版进度按“开工次数”统计，不能直接当作新版“已领取份数”；遇到提示时，请在本站空闲后点击“重置本站进度”。

网络中断等情况无法确认结果时，只暂停相关站点。可对照游戏确认现有队列归属，或在队列结束后填写本批累计已领取份数，再恢复流程。助手不会对不确定的开工请求直接重发。

原料保留改为依据实际启用的流程和锁定配方，保护各步骤下一段队列所需材料；已经提交给服务器的原料不重复预留。

## 航海

1. 打开“航海总开关”。只开启“到港自动领取”即可收取手动航程。
2. 要连续出航，明确选择已解锁航线，设置补给和伙伴，然后开启“自动再次出航”。
3. 指定了伙伴时，严格等待该队伍空闲。全部留空时，只有“未指定时选择空闲伙伴”开启才自动选人，人数可设 1～3。
4. 可以为航海保留指定伙伴，避免空闲时被生产系统占用。已经在其他岗位工作的伙伴不会被自动撤下。

**自动建船、自动改装、自动出航默认关闭，各自独立开启。** 改装目标可设 1～3 级；每轮最多改装一项。建船、改装和出航共用每轮金币预算，同时受金币保底约束，出航另受体力保底与安全补给库存约束。

默认每轮金币预算 1000，金币保底 1000，出航后体力保底 10。预算 0 表示不执行有费用的操作。修改参数后，下一轮按新参数规划。

出航使用官方接口的 `request_id`。响应不确定时保留出航记录，先同步船只状态；当前没有可确认的航程时会要求在航海面板核对，防止重复消费。

## 均衡饲料

只购买并投入商店中的 **“均衡饲料”**，不使用其他作物或食材。

- **自动补充饲料**：独立于水产总开关，畜牧使用共用饲料槽时也可运行。
- **库存不足自动购买**：可独立关闭；关闭后只使用现有的均衡饲料。
- **上下限单位**：可选择容量百分比或饲料份数。
- **触发底限 / 填充至**：默认 20% → 80%。余量达到或低于底限时开始补充，持续补到目标。
- **购买预算 / 金币保底**：默认每轮最多花 1000 金币，并至少留下 1000 金币。预算 0 时不购买。

例如容量 1000 份、底限 20%、目标 80%，会在余量不高于 200 份时补至至少 800 份。饲料按整件投入，可能略超过目标，但不会超过容量。若本轮预算不够，之后继续补到目标，不必再次降到底限。

每件可转换多少份，以服务器饲料输入列表为准。如果当前没有该信息，会先购买一件识别；仍缺少信息时停止继续购买。为传送门、委托、实际加工计划预留的物资仍受到保护；专用饲料不套用普通售卖物资的默认保留 5 个规则，但遵守显式物品保留量。

## 其他开关

生产页包含农场收获/种植/买种、采集与采矿领取/开工、鱼塘捞鱼/补苗/挖塘、垂钓、畜牧照料/收取和各产业派驻开关。设置页包含每日委托、转发接单、成就领取、特殊道具及自动换人开关。

整个助手停止后不再安排自动操作；已经送达游戏服务器的请求和队列仍由游戏处理。

## 验证

```powershell
node --check '红叶镇物语自动助手.user.js'
node tests/red-leaf-town-v4.test.cjs
```

回归测试使用 Node 自带模块和模拟 HTTP，无额外依赖、不访问游戏账号。覆盖加工计数与恢复、队列取消、道具保留、饲料购买与库存保护、航海预算和伙伴占用等场景。

`tests/red-leaf-town-ui.cjs` 使用端口 9236 的临时 Chrome 调试实例检查真实 DOM、六页切换、开关保存及桌面/手机布局，并输出离线预览截图。启动时应使用单独的临时浏览器配置目录和 headless 模式，避免影响日常浏览器。

开发时核对的官网构建为 `index-DCECfXs_.js`。目前通过离线回归和真实浏览器 UI 检查，尚未对登录账号执行实机生产、购买或航海测试。
